# 终端远程控制改进计划

## 目标

根据当前需求列表，沿用现有飞书远程控制链路，完成以下改进：

1. 项目管理终端在被监督终端所在位置打开。
2. 支持从飞书关闭终端。
3. 定位并修复 Grok 终端显示异常。
4. 提供集终端选择、信息输入发送和终端信息刷新于一体的控制界面，并精简返回文本。

> 当前对第 1 项的理解是：项目管理终端不再新建独立工作区，而是在所选被监督终端所在窗格中新建标签。若实际要求不同，实施前需重新确认位置规则。

## 近期变更与现状

近期 supervisor 相关改动已经完成：

- 远程任务和监督消息的可靠投递及多行输入。
- 上下文恢复、配置持久化和“完成后待续”状态。
- 飞书终端列表、任务发送、终端末尾 40 行读取和手动刷新。
- 项目管理 Grok 终端创建，但目前固定新建独立工作区。

当前主要缺口：

- 飞书端没有关闭任务终端的入口。
- “发送任务”和“查看终端界面”仍是两个独立页面。
- Grok 显示异常尚缺少明确复现和针对性测试。

现有能力主要位于：

- `src/renderer/pipe-bridge.ts`：终端创建、列表、读取、发送及远程控制桥。
- `src/main/feishu-supervisor.ts`：飞书卡片、权限检查和交互路由。
- `src/renderer/store/surface-slice.ts`：终端标签关闭和 PTY 回收。

## 实施计划

### 1. 统一终端定位能力

- 提取按 `surfaceId` 定位终端的内部方法，统一返回 `workspaceId`、`paneId` 和终端信息。
- 让创建、关闭、读取和发送逻辑复用同一定位入口。
- 对终端不存在、已经关闭、属于其他窗口或属于专属监督 AI 的情况返回明确错误。

### 2. 调整项目管理终端位置

- 创建前选择一个被监督任务终端作为锚点。
- 在锚点终端的 `workspaceId + paneId` 中通过现有 `addSurface` 创建项目管理终端标签，并自动选中。
- 保留固定工作目录、Grok 启动方式、`/inspect-project-progress` 首条任务和全局唯一性。
- 只有一条监督通道时直接使用；存在多条通道时显示锚点终端选择卡。
- 项目管理终端已经存在时，准确切换到其工作区、窗格和标签，不创建副本。

### 3. 支持关闭终端

- 在飞书控制首页增加“关闭终端”入口。
- 采用“选择终端 → 展示状态和影响 → 二次确认”的交互流程。
- 禁止通过此入口直接关闭专属监督 AI 终端。
- 如果目标任务终端正在被监督，确认卡明确提示将同时停止对应监督通道。
- 执行时先记录审计并解除监督绑定、关闭对应专属监督终端，再调用现有 `closeSurface` 关闭任务终端。
- 复用现有 PTY 回收及最后一个标签关闭工作区的逻辑，不自行实现第二套关闭流程。
- 关闭终端不删除任务目录和历史审计记录。

### 4. 定位并修复 Grok 显示异常

分别复现以下场景：

- 普通 Grok 直连终端。
- 项目管理 Grok 终端。
- Grok 作为专属监督 AI 的终端。

重点检查：

- 标签名称、终端类型和远程终端筛选是否正确。
- Grok 全屏 TUI 的 alternate buffer 是否读取正确。
- 中文宽字符、换行或重复刷新帧是否造成桌面端或飞书卡片错位。
- Grok hook 状态是否被错误显示为长期“执行中”或“未知”。

根据复现结果，在终端读取、文本裁剪、卡片显示或状态映射的实际问题层做最小修复，并添加对应回归测试；不在原因未明时增加 Grok 专属兜底。

### 5. 合并终端查看、发送与刷新

新增统一的“终端控制”卡片：

- 先选择需要控制的任务终端。
- 在同一张卡中展示终端名称、忙闲状态、抓取时间和最新核心文本。
- 提供多行输入框和“发送”按钮。
- 提供“刷新”按钮，继续读取当前选中的终端。
- 发送操作复用现有忙碌终端二次确认、用户输入优先和可靠投递逻辑。
- 刷新不得意外提交输入；需要验证飞书更新卡片时对未发送草稿的处理行为。

返回文本采用确定性精简方式：

- 保留最新非空内容，优先保留末尾错误、结果和提示符附近文本。
- 折叠重复空行和明显重复的刷新画面。
- 默认限制为约 12～20 行、1200～1600 字。
- 不调用额外 AI 总结，避免增加延迟、费用或内容失真。

### 6. 测试与文档

补充以下测试：

- 项目管理终端落在正确窗格、自动选中且重复创建不产生副本。
- 关闭普通终端、最后一个终端、被监督终端和过期 `surfaceId`。
- Grok 显示异常的真实复现用例。
- 终端控制卡的选择、刷新、发送、忙碌确认、权限限制和重复点击防护。
- 核心文本裁剪能够保留最新错误或结果。

同步更新 `docs/feishu-ai-supervisor.md` 中的日常操作、安全边界和关闭行为说明。

## 实施顺序

1. 统一终端定位能力。
2. 调整项目管理终端位置。
3. 增加安全关闭终端能力。
4. 复现并修复 Grok 显示异常。
5. 合并终端查看、发送与刷新卡片。
6. 更新文档并完成完整验证。

每个阶段应保持可独立测试和回滚，避免将位置调整、关闭语义和 Grok 修复混入同一批改动。

## 验证方式

按以下顺序执行：

1. 运行与改动直接相关的 Vitest 单测。
2. 运行 `npm run typecheck`。
3. 运行 `npm run lint`。
4. 运行 `npm test`。

对于飞书卡片交互，还需在白名单用户单聊中手动验证：终端选择、刷新、发送、忙碌确认、关闭确认及权限隔离。
